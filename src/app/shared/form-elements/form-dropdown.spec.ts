/* tslint:disable:no-unused-variable */

import { async, inject } from '@angular/core/testing';
import {FormDropdown} from './form-dropdown';

describe('FormDropdown', () => {
  it('should create an instance', () => {
    expect(new FormDropdown()).toBeTruthy();
  });
});
