/* tslint:disable:no-unused-variable */

import { async, inject } from '@angular/core/testing';
import {FormElement} from './form-element';

describe('FormElement', () => {
  it('should create an instance', () => {
    expect(new FormElement()).toBeTruthy();
  });
});
